# 第三章 系统架构设计

## 3.1 概述

系统架构设计是在线考试系统开发的核心环节，它决定了系统的性能、可维护性、可扩展性和安全性。本章将详细介绍本在线考试系统的架构设计，包括总体架构、分层架构、数据库设计、接口设计等方面。系统采用现代化的分层架构模式，结合微服务思想，确保系统的高内聚、低耦合特性。

## 3.2 总体架构设计

### 3.2.1 架构原则

本系统架构设计遵循以下原则：

1. **分层原则**：采用经典的分层架构，将系统分为表示层、业务逻辑层、数据访问层和数据存储层。

2. **模块化原则**：系统采用模块化设计，每个模块职责单一，便于维护和扩展。

3. **可扩展性原则**：架构设计考虑系统的可扩展性，支持水平扩展和垂直扩展。

4. **安全性原则**：在架构设计中充分考虑安全性，包括认证、授权、数据加密等。

5. **性能原则**：通过缓存、异步处理等技术手段提高系统性能。

### 3.2.2 总体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                    客户端层 (Client Layer)                    │
├─────────────────────────────────────────────────────────────┤
│  Web前端  │  移动端  │  管理端  │  第三方系统  │  其他客户端    │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                    API网关层 (API Gateway)                   │
├─────────────────────────────────────────────────────────────┤
│  负载均衡  │  路由转发  │  限流控制  │  安全认证  │  日志记录    │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                  应用服务层 (Application Layer)              │
├─────────────────────────────────────────────────────────────┤
│  用户服务  │  考试服务  │  题目服务  │  成绩服务  │  通知服务    │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                   业务逻辑层 (Business Layer)                │
├─────────────────────────────────────────────────────────────┤
│  认证授权  │  权限控制  │  业务规则  │  数据验证  │  异常处理    │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                   数据访问层 (Data Access Layer)             │
├─────────────────────────────────────────────────────────────┤
│  ORM映射  │  数据缓存  │  事务管理  │  连接池  │  查询优化    │
└─────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据存储层 (Storage Layer)                │
├─────────────────────────────────────────────────────────────┤
│  PostgreSQL  │  Redis  │  文件存储  │  日志存储  │  备份存储    │
└─────────────────────────────────────────────────────────────┘
```

### 3.2.3 技术架构

系统采用以下技术架构：

1. **运行时环境**：Node.js + TypeScript
2. **Web框架**：Koa.js
3. **数据库**：PostgreSQL (主数据库) + Redis (缓存)
4. **ORM框架**：TypeORM
5. **认证授权**：JWT + Casbin
6. **消息队列**：Redis Pub/Sub
7. **实时通信**：WebSocket
8. **日志系统**：Winston
9. **测试框架**：Jest
10. **部署工具**：Docker + PM2

## 3.3 分层架构设计

### 3.3.1 表示层 (Presentation Layer)

表示层负责处理用户请求和响应，包括：

1. **路由管理**：使用Koa Router管理API路由
2. **请求解析**：解析HTTP请求参数和请求体
3. **响应格式化**：统一格式化API响应
4. **错误处理**：处理请求过程中的异常
5. **中间件处理**：执行认证、日志、限流等中间件

**核心组件：**
- `app.ts`：应用入口，配置中间件和路由
- `route/index.ts`：路由配置
- `middleware/`：各种中间件实现

### 3.3.2 业务逻辑层 (Business Logic Layer)

业务逻辑层包含系统的核心业务逻辑，包括：

1. **用户管理**：用户注册、登录、权限管理
2. **考试管理**：考试创建、编辑、发布、监控
3. **题目管理**：题目创建、编辑、分类、导入导出
4. **成绩管理**：成绩计算、统计、分析
5. **系统管理**：配置管理、日志管理、监控

**核心组件：**
- `controller/`：控制器，处理HTTP请求
- `service/`：服务层，实现业务逻辑
- `dto/`：数据传输对象，定义接口数据结构

### 3.3.3 数据访问层 (Data Access Layer)

数据访问层负责数据的持久化和访问，包括：

1. **实体映射**：使用TypeORM定义数据库实体
2. **数据操作**：CRUD操作和复杂查询
3. **事务管理**：确保数据一致性
4. **缓存管理**：Redis缓存策略
5. **连接池管理**：数据库连接池优化

**核心组件：**
- `orm/entity.ts/`：数据库实体定义
- `orm/repository/`：数据访问仓库
- `orm/BaseRepo.ts`：基础仓库类

### 3.3.4 数据存储层 (Storage Layer)

数据存储层提供数据持久化服务，包括：

1. **关系型数据库**：PostgreSQL存储结构化数据
2. **缓存数据库**：Redis提供高速缓存
3. **文件存储**：存储上传的文件和图片
4. **日志存储**：系统运行日志和操作日志

## 3.4 数据库设计

### 3.4.1 数据库架构

系统采用PostgreSQL作为主数据库，Redis作为缓存数据库的混合架构：

1. **PostgreSQL**：存储用户信息、考试数据、题目内容等持久化数据
2. **Redis**：存储会话信息、缓存数据、限流数据等临时数据

### 3.4.2 核心实体设计

#### 3.4.2.1 用户相关实体

**Account (用户账户)**
```sql
CREATE TABLE account (
    id SERIAL PRIMARY KEY,
    user_name VARCHAR(255) UNIQUE NOT NULL,
    email VARCHAR(255) UNIQUE,
    phone VARCHAR(255) UNIQUE,
    password VARCHAR(255) NOT NULL,
    salt VARCHAR(255) NOT NULL,
    role INTEGER,
    access_token VARCHAR(255),
    is_active BOOLEAN DEFAULT TRUE,
    avatar VARCHAR(255),
    id_card VARCHAR(255),
    nick_name VARCHAR(255),
    status INTEGER DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP
);
```

**Role (角色)**
```sql
CREATE TABLE role (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) UNIQUE NOT NULL,
    description TEXT NOT NULL,
    status INTEGER DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP
);
```

#### 3.4.2.2 考试相关实体

**Exam (考试)**
```sql
CREATE TABLE exam (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    description TEXT,
    start_time TIMESTAMP,
    end_time TIMESTAMP,
    duration INTEGER,
    total_score INTEGER,
    pass_score INTEGER,
    max_attempts INTEGER DEFAULT 1,
    is_published BOOLEAN DEFAULT FALSE,
    created_by INTEGER NOT NULL,
    updated_by INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP
);
```

**Question (题目)**
```sql
CREATE TABLE question (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    img TEXT,
    type INTEGER NOT NULL,
    difficulty INTEGER DEFAULT 0,
    options JSON,
    answer TEXT,
    score INTEGER NOT NULL,
    status INTEGER DEFAULT 1,
    created_by INTEGER NOT NULL,
    updated_by INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP
);
```

**ExamQuestion (考试题目关联)**
```sql
CREATE TABLE exam_question (
    id SERIAL PRIMARY KEY,
    exam_id INTEGER NOT NULL,
    question_id INTEGER NOT NULL,
    order_index INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP
);
```

**ExamAnswer (考试答案)**
```sql
CREATE TABLE exam_answer (
    id SERIAL PRIMARY KEY,
    exam_id INTEGER NOT NULL,
    question_id INTEGER NOT NULL,
    user_id INTEGER NOT NULL,
    answer TEXT,
    score INTEGER,
    is_correct BOOLEAN,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP
);
```

#### 3.4.2.3 系统相关实体

**Config (系统配置)**
```sql
CREATE TABLE config (
    id SERIAL PRIMARY KEY,
    key VARCHAR(100) UNIQUE NOT NULL,
    value TEXT,
    description VARCHAR(255),
    status INTEGER DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP
);
```

**Log (系统日志)**
```sql
CREATE TABLE log (
    id SERIAL PRIMARY KEY,
    level VARCHAR(20) NOT NULL,
    message TEXT NOT NULL,
    role VARCHAR(50),
    path VARCHAR(255),
    method VARCHAR(10),
    user_id INTEGER,
    res_status INTEGER,
    trace_id VARCHAR(100),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP
);
```

**RefreshToken (刷新令牌)**
```sql
CREATE TABLE refresh_token (
    id SERIAL PRIMARY KEY,
    user_id INTEGER NOT NULL,
    token VARCHAR(255) NOT NULL,
    expires_at TIMESTAMP NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    deleted_at TIMESTAMP
);
```

### 3.4.3 数据库关系设计

#### 3.4.3.1 实体关系图

```
Account (用户) ──┐
                ├── ExamAnswer (考试答案)
                └── Exam (考试) ──┐
                                 ├── ExamQuestion (考试题目关联)
                                 └── Question (题目)

Role (角色) ──── Account (用户)

Config (配置) ── 独立实体

Log (日志) ──── 独立实体

RefreshToken (刷新令牌) ──── Account (用户)
```

#### 3.4.3.2 关系说明

1. **Account ↔ Role**：多对一关系，一个角色可以有多个用户
2. **Account ↔ Exam**：一对多关系，一个用户可以参加多个考试
3. **Exam ↔ Question**：多对多关系，通过ExamQuestion关联表实现
4. **Account ↔ ExamAnswer**：一对多关系，一个用户可以有多条答案记录
5. **Exam ↔ ExamAnswer**：一对多关系，一个考试可以有多条答案记录
6. **Question ↔ ExamAnswer**：一对多关系，一个题目可以有多条答案记录

### 3.4.4 索引设计

为了提高查询性能，系统设计了以下索引：

1. **主键索引**：所有表的主键自动创建唯一索引
2. **唯一索引**：用户名、邮箱、手机号等唯一字段
3. **普通索引**：经常用于查询条件的字段
4. **复合索引**：多字段组合查询的索引

**主要索引：**
```sql
-- 用户表索引
CREATE INDEX idx_account_user_name ON account(user_name);
CREATE INDEX idx_account_email ON account(email);
CREATE INDEX idx_account_phone ON account(phone);
CREATE INDEX idx_account_role ON account(role);

-- 考试表索引
CREATE INDEX idx_exam_created_by ON exam(created_by);
CREATE INDEX idx_exam_start_time ON exam(start_time);
CREATE INDEX idx_exam_end_time ON exam(end_time);
CREATE INDEX idx_exam_is_published ON exam(is_published);

-- 题目表索引
CREATE INDEX idx_question_type ON question(type);
CREATE INDEX idx_question_difficulty ON question(difficulty);
CREATE INDEX idx_question_created_by ON question(created_by);

-- 考试答案表索引
CREATE INDEX idx_exam_answer_exam_id ON exam_answer(exam_id);
CREATE INDEX idx_exam_answer_user_id ON exam_answer(user_id);
CREATE INDEX idx_exam_answer_question_id ON exam_answer(question_id);
CREATE INDEX idx_exam_answer_exam_user ON exam_answer(exam_id, user_id);
```

## 3.5 接口设计

### 3.5.1 RESTful API设计

系统采用RESTful API设计风格，遵循以下原则：

1. **资源导向**：URL表示资源，HTTP方法表示操作
2. **无状态**：每个请求包含所有必要信息
3. **统一接口**：使用标准HTTP方法
4. **分层系统**：支持中间件和代理

### 3.5.2 API接口规范

#### 3.5.2.1 请求格式

**HTTP方法：**
- GET：获取资源
- POST：创建资源
- PUT：更新资源
- DELETE：删除资源

**请求头：**
```
Content-Type: application/json
Authorization: Bearer <token>
X-API-Key: <api-key>
```

**请求体：**
```json
{
  "field1": "value1",
  "field2": "value2"
}
```

#### 3.5.2.2 响应格式

**成功响应：**
```json
{
  "code": 0,
  "message": "success",
  "data": {
    // 响应数据
  },
  "success": true,
  "timestamp": "2024-01-01T00:00:00.000Z",
  "traceId": "uuid"
}
```

**错误响应：**
```json
{
  "code": -1,
  "message": "error message",
  "data": null,
  "success": false,
  "timestamp": "2024-01-01T00:00:00.000Z",
  "traceId": "uuid",
  "error": {
    "code": "ERROR_CODE",
    "message": "详细错误信息"
  }
}
```

### 3.5.3 核心API接口

#### 3.5.3.1 用户认证接口

```
POST /api/auth/login          # 用户登录
POST /api/auth/logout         # 用户登出
POST /api/auth/register       # 用户注册
POST /api/auth/refresh        # 刷新令牌
GET  /api/auth/profile        # 获取用户信息
PUT  /api/auth/profile        # 更新用户信息
```

#### 3.5.3.2 考试管理接口

```
GET    /api/exam              # 获取考试列表
POST   /api/exam              # 创建考试
GET    /api/exam/:id          # 获取考试详情
PUT    /api/exam/:id          # 更新考试
DELETE /api/exam/:id          # 删除考试
POST   /api/exam/:id/start    # 开始考试
POST   /api/exam/:id/submit   # 提交考试
GET    /api/exam/:id/result   # 获取考试结果
```

#### 3.5.3.3 题目管理接口

```
GET    /api/question          # 获取题目列表
POST   /api/question          # 创建题目
GET    /api/question/:id      # 获取题目详情
PUT    /api/question/:id      # 更新题目
DELETE /api/question/:id      # 删除题目
POST   /api/question/import   # 导入题目
GET    /api/question/export   # 导出题目
```

#### 3.5.3.4 成绩管理接口

```
GET    /api/score             # 获取成绩列表
GET    /api/score/:id         # 获取成绩详情
GET    /api/score/statistics  # 获取成绩统计
POST   /api/score/export      # 导出成绩
```

### 3.5.4 WebSocket接口设计

系统使用WebSocket实现实时通信功能：

#### 3.5.4.1 连接管理

```javascript
// 连接建立
ws://localhost:3000/ws

// 认证消息
{
  "type": "auth",
  "token": "jwt_token"
}

// 心跳消息
{
  "type": "ping"
}
```

#### 3.5.4.2 消息类型

```javascript
// 考试状态更新
{
  "type": "exam_status",
  "data": {
    "examId": 1,
    "status": "started",
    "remainingTime": 3600
  }
}

// 实时通知
{
  "type": "notification",
  "data": {
    "title": "考试提醒",
    "message": "考试即将开始",
    "level": "info"
  }
}

// 系统消息
{
  "type": "system_message",
  "data": {
    "message": "系统维护通知",
    "level": "warning"
  }
}
```

## 3.6 安全架构设计

### 3.6.1 认证机制

系统采用JWT (JSON Web Token) 进行用户认证：

1. **登录认证**：用户提供用户名和密码，系统验证后生成JWT令牌
2. **令牌验证**：每个请求携带JWT令牌，系统验证令牌有效性
3. **令牌刷新**：使用刷新令牌机制延长用户会话
4. **令牌黑名单**：支持令牌撤销和黑名单机制

### 3.6.2 授权机制

系统采用基于角色的访问控制（RBAC）模型：

1. **角色定义**：定义系统角色（管理员、教师、学生等）
2. **权限分配**：为每个角色分配相应的权限
3. **权限检查**：使用Casbin进行权限验证
4. **动态权限**：支持运行时权限策略更新

### 3.6.3 数据安全

1. **密码加密**：使用bcrypt对用户密码进行哈希加密
2. **数据传输**：支持HTTPS加密传输
3. **敏感数据**：对敏感数据进行加密存储
4. **SQL注入防护**：使用ORM防止SQL注入攻击

### 3.6.4 系统安全

1. **API限流**：使用令牌桶算法限制API访问频率
2. **防爬虫**：实现反爬虫机制，防止恶意访问
3. **输入验证**：对所有用户输入进行验证和过滤
4. **错误处理**：避免敏感信息泄露

## 3.7 性能架构设计

### 3.7.1 缓存策略

系统采用多级缓存策略：

1. **应用缓存**：使用Redis缓存频繁访问的数据
2. **查询缓存**：缓存复杂查询结果
3. **会话缓存**：缓存用户会话信息
4. **静态资源缓存**：使用CDN缓存静态资源

### 3.7.2 数据库优化

1. **索引优化**：合理设计数据库索引
2. **查询优化**：优化SQL查询语句
3. **连接池**：使用数据库连接池
4. **读写分离**：支持主从数据库架构

### 3.7.3 异步处理

1. **异步I/O**：充分利用Node.js的异步特性
2. **消息队列**：使用Redis实现消息队列
3. **任务调度**：使用Bull实现任务调度
4. **事件驱动**：使用事件驱动架构

## 3.8 监控和日志架构

### 3.8.1 日志系统

系统采用结构化日志记录：

1. **应用日志**：记录应用程序运行日志
2. **访问日志**：记录用户访问日志
3. **错误日志**：记录系统错误和异常
4. **审计日志**：记录重要操作日志

### 3.8.2 监控系统

1. **性能监控**：监控系统性能指标
2. **业务监控**：监控业务关键指标
3. **错误监控**：监控系统错误率
4. **资源监控**：监控系统资源使用情况

## 3.9 本章小结

本章详细介绍了在线考试系统的架构设计，包括总体架构、分层架构、数据库设计、接口设计、安全架构、性能架构等方面。系统采用现代化的分层架构模式，结合多种技术手段，确保了系统的高性能、高可用性、高安全性和可扩展性。下一章将详细介绍系统的具体实现过程，展示这些架构设计在实际代码中的体现。
